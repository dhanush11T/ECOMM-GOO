{
  "version": 2,
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" } // Assuming your backend functions are in the api directory
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
